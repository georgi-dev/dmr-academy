extends ../../layouts/dashboard.pug

block layout
  +navbar(server, 'dark')       

  +sidebar-secondary(server, page['view'], 'dark')   

  +content('dark', 'without-sidebar')(class='-collapsible') 
    block content
      +container-fluid
        +row 
          +col({ lg: 12 })
            +page-heading(server, page)
